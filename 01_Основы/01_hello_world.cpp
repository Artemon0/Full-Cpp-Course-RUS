/*
 * Урок 1: Первая программа на C++
 * Тема: Hello World и базовая структура программы
 */

#include <iostream>  // Подключение библиотеки ввода-вывода
#include <windows.h> // Для работы с кодировкой в Windows
using namespace std; // Использование стандартного пространства имен

int main() {
    // Установка кодировки UTF-8 для корректного отображения русского текста
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);
    // Вывод текста на экран
    cout << "Привет, мир!" << endl;
    cout << "Добро пожаловать в C++!" << endl;
    
    return 0; // Успешное завершение программы
}

/*
 * ОБЪЯСНЕНИЕ:
 * 
 * #include <iostream> - подключает библиотеку для работы с вводом/выводом
 * using namespace std - позволяет использовать cout без префикса std::
 * int main() - главная функция программы, точка входа
 * cout - объект для вывода данных в консоль
 * << - оператор вставки (отправляет данные в поток)
 * endl - переход на новую строку
 * return 0 - возвращает 0, означает успешное выполнение
 * 
 * ЗАДАНИЕ:
 * 1. Измените text приветствия
 * 2. Добавьте еще несколько строк вывода
 * 3. Попробуйте print свое name и age
 */
